#include<bits/stdc++.h>
using namespace std; 

const int MAXN = 2e5 + 1; 
int a[MAXN], b[MAXN][2]; 

int main(){
  int n, m, k, aux; 
  cin >> n >> m >> k;
  for (int i = 0; i<n; i++){
    cin >> aux; 
  }

  return 0; 
}