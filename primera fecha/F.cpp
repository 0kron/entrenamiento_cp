#include<bits/stdc++.h>

using namespace std; 
const int MAXN = 1e5+2; 
int B[MAXN]; 

int main(){
  int N, K, l, r; 
  char O; 
  vector<int> maximos; 
  scanf("%d %d", &N, &K); 
  for (int i = 0; i<N; i++){
    cin >> B[i]; 
  }
  for (int i = 0; i <K; i++){
    scanf("%c %d %d", &O, &l, &r); 
    if (O == 'Q')
  }


  return 0; 
}